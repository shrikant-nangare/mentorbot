apiVersion: v1
kind: Service
metadata:
  name: mentorbot
  namespace: mentorbot
  labels:
    app.kubernetes.io/name: mentorbot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mentorbot
  ports:
    - name: http
      port: 80
      targetPort: http

